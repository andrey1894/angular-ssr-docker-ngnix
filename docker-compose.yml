version: '3.7'

services:
  angular-app:
    build: .
    container_name: angular-app
    restart: always
    ports:
      - '4000:4000'
    command: 'yarn serve:ssr:client'

  nginx:
    build: ./nginx
    ports:
      - '80:80'
    depends_on:
      - angular-app
